name: Blueprint Pages

on:
  pull_request:
  push:
    branches:
      - main
      - notion-display-from-before-miniblueprint-2026-08-26
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  pages:
    # Reusable workflow builds and deploys the Blueprint site.
    uses: ./.github/workflows/blueprint-pages.yml
    secrets: inherit
